# Chaos Engineering Scripts

| Script | Effect |
|--------|--------|
| `kafka-outage.ps1` | Stop/start Kafka container |
| `kill-service.ps1` | Kill one background service by name |

Requires services started via `run-services.ps1`.
