# GitHub Actions

**Phase 5+**

Planned workflows:

- `ci.yml` — lint, test, build Docker images
- `contract-validation.yml` — OpenAPI/AsyncAPI spectral
- `deploy-staging.yml` — Helm deploy on main merge
